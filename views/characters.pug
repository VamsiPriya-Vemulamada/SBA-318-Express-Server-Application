html
  head
    title Hero and Heroine App
  body
    h1 List of Heroes and Heroines
    ul
      each character in characters
        li #{character.role}: #{character.name}
    h2 Add a New Character
    form(action='/add-character' method='POST')
      label(for='name') Character Name:
      input(type='text' name='name' required)
      label(for='role') Role (Hero or Heroine):
      input(type='text' name='role' required)
      button(type='submit') Add Character